// Define an array containing the allowed origins for CORS requests.
// '*' indicates that all origins are allowed. Modify this as per your security requirements.
const allowedOrigins = ['*'];

// Export the allowedOrigins array for use in other modules.
module.exports = allowedOrigins;
